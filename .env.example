# Vercel / Neon server-side only. Do not expose this in script.js.
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DB?sslmode=require

# Optional: protect the daily Vercel Cron endpoint.
CRON_SECRET=change-me
